{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "build",
      "command": "make", // Make sure this runs the correct build system
      "options": {
        "cwd": "${workspaceFolder}" // Ensure it runs from your project folder
      },
      "problemMatcher": [
        "$gcc" // This will capture GCC compilation errors
        // Optional: Use if you're using Clang instead of GCC
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Task generated by Debugger."
    }
  ]
}
